--- a/src/mach.h	2026-09-24 21:54:18
+++ b/src/mach.h	2026-09-24 21:54:18
@@ -154,14 +154,20 @@
   msg.descriptor.type = MACH_MSG_OOL_DESCRIPTOR;
 
   mach_msg_return_t ret = mach_msg(&msg.header,
-                                   MACH_SEND_MSG,
+                                   MACH_SEND_MSG | MACH_SEND_TIMEOUT,
                                    sizeof(struct mach_message),
                                    0,
                                    MACH_PORT_NULL,
-                                   MACH_MSG_TIMEOUT_NONE,
+                                   1000,
                                    MACH_PORT_NULL              );
   
-  if (ret != KERN_SUCCESS) return NULL;
+  if (ret != KERN_SUCCESS) {
+    if (response) {
+      mach_port_mod_refs(task, response_port, MACH_PORT_RIGHT_RECEIVE, -1);
+      mach_port_deallocate(task, response_port);
+    }
+    return NULL;
+  }
 
   char* rsp = NULL;
   if (response) {
